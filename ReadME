the web is made up of html files and other resources that are linked together. the html, css and javascriptar stored on servers. Hypertext are referenced which are joined by links. to load a web page, your browser sends a request to the server that houses your files which are stored under a dns. A browser communicates with the servers to ge the files.